@model List<Webkho_20241021.Models.khotongs>
@{
    ViewData["Title"] = "Tongkho";
    Layout = "~/Areas/TruongBPMuahang/Views/Layout/Layout.cshtml";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="~/CSS/Action.css" rel="stylesheet" />
    <link href="~/CSS/Kho.css" rel="stylesheet" />
    <link href="~/CSS/style.css" rel="stylesheet" />
    <link href="~/CSS/Table.css" rel="stylesheet" />
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet" />

    <title>Document</title>
</head>
<body>
    <nav class="bodykho">
        <div class="menu-kho">
            <ul>
                <li><a href="">Tổng kho</a></li>
                <li><a href="">Kho dự án</a></li>
            </ul>
            <ul>
                <input type="text" id="timkiem" name="Timkiem" placeholder="Tìm kiếm" />
            </ul>
        </div>
        <div class="Tablekhotong-box">
            <table class="tablekhotong">
                <thead>
                    <tr>
                        <td>Tên vật tư</td>
                        <td>Mã vật tư</td>
                        <td>Mã kho</td>
                        <td>
                            <select id="hangsx-filter">
                                <option value="">Hãng SX</option>
                                @foreach (var item in Model.Select(m => m.HangSX).Distinct())
                                {
                                    <option value="@item">@item</option>
                                }
                            </select>
                        </td>
                        <td>Nhà cung cấp</td>
                        <td>Số lượng</td>
                        <td>Đơn vị</td>
                        <td>Ngày nhập kho</td>
                        <td>Ngày bảo hành</td>
                        <td>Thời gian BH</td>
                        <td>Trạng thái</td>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int STT = 1;
                    }
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.TenSanpham</td>
                            <td>@item.MaSanpham</td>
                            <td>@item.Makho</td>
                            <td>@item.HangSX</td>
                            <td>@item.NhaCC</td>
                            <td>@item.SL</td>
                            <td>@item.DonVi</td>
                            <td>@item.NgayNhapkho?.ToString("dd/MM/yyyy")</td>
                            <td>@item.NgayBaohanh?.ToString("dd/MM/yyyy")</td>
                            <td>@item.ThoiGianBH?.ToString("dd/MM/yyyy")</td>
                            <td>@item.TrangThai</td>
                        </tr>
                        STT++;
                    }
                </tbody>
            </table>
        </div>
        <div id="yeucau-action">
            <div class="action">
                <ul class="hanhdong">
                    <li>
                        <a href="/TruongBPMuahang/Home/Themthietbi">Thêm</a>
                    </li>
                </ul>
                <ul class="excel">
                    <li>
                        <a href="/TruongBPMuahang/Home/Import">Import</a>
                    </li>
                    <li>
                        <a href="">Export</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <script src="~/js/khotong.js"></script>
</body>
</html>
